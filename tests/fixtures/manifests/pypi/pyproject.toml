[project]
name = "sample-pkg"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.28.0",
  "pyyaml",
  "click==8.1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0",
  "mypy",
]
docs = [
  "sphinx>=6.0",
]
